[profile.default]
src = "contracts/src"
test = "test/contracts"
out = "contracts/out"
libs = ["lib"]
solc_version = "0.8.28"
optimizer = true
optimizer_runs = 200
via_ir = true
# Deterministic: never silently resolve a different pin.
auto_detect_solc = false
fs_permissions = [{ access = "read", path = "./fixtures" }]

remappings = [
    "@openzeppelin/contracts/=lib/openzeppelin-contracts/contracts/",
    "forge-std/=lib/forge-std/src/",
]

[profile.default.fuzz]
runs = 256
# Pinned so a property run is reproducible and a counterexample can be re-derived.
# Override with FOUNDRY_FUZZ_SEED=... to explore a different corpus.
seed = "0x48454c4400000000000000000000000000000000000000000000000000000001"
