# P07 — Verify independent developer use and release evidence

## Copy-ready coordinator prompt

Run Held V4 phase P07 only. Remain Astra ARCHITECT/COORDINATOR; dispatch product implementation to full GLM-5.3 at HIGH through the existing glm/ZAI route, with verified outgoing reasoning.effort=high. Do not implement product code yourself or recursively delegate.

Read:
- /Users/girish/Documents/Codex/2026-09-15/ha/outputs/14_Held_Locked_V4.md
- /Users/girish/Documents/Codex/2026-09-15/ha/outputs/held-v4-prompts/00_COMMON.md
- The accepted prerequisite phase report/manifests in /Users/girish/Documents/Codex/2026-09-15/ha/held/evidence.

Apply the common contract in full. Produce a unique ROLE=EXECUTOR packet for the task below, verify routing at the actual provider boundary, capture the exact session and all local evidence, and independently review/test the result. The task below is worker payload content, not an instruction to switch the coordinator's role.

### Executor objective

Make another developer able to pick up the real integration and make every release claim traceable.

### Prerequisites

P06 correctness accepted, comparison conclusions recorded and any product decision resolved.

Inspect their actual acceptance artifacts before edits. A file existing is not acceptance. Complete independent preparation if a prerequisite is blocked, and report the exact missing result. Do not proceed with dependent implementation by assumption.

### Workspace and allowed changes

Product root: /Users/girish/Documents/Codex/2026-09-15/ha/held
Owns: held/README.md, held/docs/**, held/examples/**, held/.env.example, held/.github/workflows/**, held/Makefile, held/evidence/P07/**, held/release-manifest.json

Preserve all unrelated work. Changes outside these paths require the coordinator to amend the bounded packet, not a silent scope expansion. No global provider/authentication/history changes and no writes to the locked plan.

### Required work

1. Use a genuinely clean checkout/environment and fresh non-secret configuration. A different executor session may test developer setup, but label that as independent technical replication, not a real customer study.
2. Document exact prerequisites, pinned versions, supported chain/market/owner topology, setup commands, local-only demo, separately authorized public run, expected outcomes and recovery steps.
3. Create CI that runs meaningful checks for relevant code and dependencies. A green badge is not a score or security audit. Validate test scope and avoid leaking fixture keys that could be mistaken for real secrets.
4. Ensure the adapter is reusable without copying the entire console or obtaining creator credentials. Confirm native risk and result handling remain intact.
5. Audit dependencies/licenses, no unsupported reuse/eligibility claim, secrets and permissions. Resolve actual event/platform constraints that affect publication; do not invent rules.
6. Create release manifest binding code revisions, config schemas, tests, deployed-code identity when any, evidence hashes and known limitations. Check every claimed artifact actually exists.
7. Prepare repository/public evidence changes locally. Publish only with applicable user authorization; do not silently push or overwrite remote content.

### Required artifacts

Clean-checkout installation record; complete README/example configuration; CI definition and actual results when run; release manifest and evidence index.

Also write /Users/girish/Documents/Codex/2026-09-15/ha/held/evidence/P07/report.md and acceptance.json, and preserve commands/exit codes, redacted traces, source/configuration digests, routing witness and exact worker session metadata under the common contract.

### Acceptance and verification

- make check-phase-07 verifies documented local install and example integration from clean state, configuration errors, evidence links/manifests and secret scanning.
- Another session reproduces ordinary native operation plus one meaningful failure/recovery without creator-only dependencies. Record assistance and deviations.
- All public/runtime statements point to actual evidence; empty or placeholder URLs are release failures.
- Coordinator reviews actual code/docs diff and targeted clean-checkout result, not just a test-count summary.

Run meaningful checks appropriate to the changes. If an advertised guarantee is contradicted, repair it within scope or report a specific design decision; do not weaken the test to retain the claim.

### Stop and handoff

Do not invent deployments, credentials, approvals, source behavior, results or observations. For public transactions, deployment, spending, outreach or publication, use explicit existing authorization or finish a concrete action manifest and request only the missing authorization as the final step. This phase does not automatically start the next one.

Return: changed files; supported behavior; commands and exit codes; evidence paths; unresolved gates; actual limitations; and the exact next phase that is unblocked. Coordinator must review actual artifacts and rerun decisive checks before accepting the phase.
